import './styles.css'; // Optional: if you have a CSS file for styling
import loadHome from './home'; // Import the loadHome function

// Call the function to render the home page
document.addEventListener('DOMContentLoaded', () => {
  loadHome();
});
